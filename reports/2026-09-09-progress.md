# Progress — week of Sep 8 (as of Fri 2026-09-11)

Running log of execution against `2026-09-08-plan.md`. Feeds the Sun 2026-09-14 weekly report.

## Repo

`65739dd..4a02dd0` is on `origin/master` and deployed live on Railway (deploy
`bf58e654`, 2026-09-11) — first time the 2026-09-09 SMS/kiosk work (pre-charge
confirmation, plain-text SMS, onboarding reorder, real login verification) has
run anywhere but the Pi. Pi and origin still in sync separately.

## Plan days

### Day 1 — saved-order record from the real cart ✅ (`de404ca`)
`checkoutCurrentCart()` now scrapes the checkout page's itemised order list
(`scrapeCheckoutLineItems()`) and returns it as `lineItems` on every success path.
`server.js` `reconcileOrderItems()` builds the saved order record from that when
present — carrying each local cart item's `id` / `selectedOptions` / `source` by
normalised name match so `[REORDER]` still works — and falls back to the local
cart when the scrape is empty. Wired into voice `finalizeDoordashCheckout()` and
touch `POST /api/food/checkout`. 9 unit cases pass.
**Unverified:** the checkout-page scrape itself — the Pi browser can't load
DoorDash's checkout page. Rides with the Day 4 Railway session.

### Day 2 — multi-restaurant smoke test ✅ (2026-09-11, findings only, no code — `4a02dd0`)
Search → select → add across Wingstop, Costa Vida, Little Caesars, Taco Time,
and Cafe Rio (new), each verified against the real cart via the `my cart` voice
phrase / `actions` marker. 4 of 5 added a real item cleanly (Costa Vida, Little
Caesars, Taco Time, Cafe Rio); no hangs, no silent wrong-adds, no charge risk
(`DOORDASH_DRY_RUN=true` on the Pi throughout).
**New finding:** the known Costa-Vida-"Single Taco" hidden-required-option-group
bug is wider than thought — Wingstop's Cajun Fried Corn and Veggie Sticks both
hit it too. `itemPage`'s `optionLists` (HTTP fast path) AND the Playwright DOM
extraction both report only the FIRST required group; `addCartItemV2` then
rejects on a second, never-surfaced group ("Corn Seasoning Options" / "Modify
Veggie Sticks"). Likely affects most Wingstop items with a seasoning/modify
sub-group, not just these two. Suggested fix (not yet done): on
`item_validation_error`, parse the named group out of the API error and
re-surface it as an additional required group — same fix now confirmed general-
purpose, not Costa-Vida-specific.
Also reproduced the Day-5 `readBrowserCart()` name-mash bug fresh on Cafe Rio
("Chips" → `"1x Chips$6.382 ×2 ×2 × - $0.00"`), and one unplanned server
auto-restart (Xvfb/Chromium fragility, recovered on its own).

### Day 3 — SMS `[SELECT]` split-brain ✅ (`ed9247a`)
Root cause: the SMS `[SELECT: N]` handler only wrote `prefs.currentRestaurant`
inside the menu-load success branches, only `cacheCurrentRestaurant()` on the
scrape path's success branch, and reverted `currentRestaurant` to null on an
empty menu. So a 2nd select whose menu scrape failed left `prefs` on the new
pick while `getCachedCurrentRestaurant()` still returned the old one — later
`[ADD_ITEM_NUM]`s landed on the previous restaurant. Reproduced live on the Pi
via `/api/message` (`prefs=157395/Cafe Rio` vs `cache=157397/Costa Vida`).
Fix: persist `prefs` + `cacheCurrentRestaurant` together the moment the
selection resolves, before the menu load; drop the null revert. Mirrors
`/api/food/select`. Verified both fast-path and scrape-fail 2nd selects — no
split brain either way.

### Day 4 — Railway pre-charge scrape ⏳ paused mid-session (2026-09-11), not done
Also carries: verify Day 1's `scrapeCheckoutLineItems()` output, and the kiosk
pre-charge confirmation, against a real checkout page.

Deployed `4a02dd0` to Railway (built + deployed cleanly, prewarm OK modulo a
transient CF challenge timeout on the homepage — non-fatal, expected). Then
worked an SMS flow on two fresh test numbers (`+15550199010`, `+15550199011`)
toward a real `[PLACE_ORDER]` preview: onboarding → `[SETUP_DOORDASH]` with the
already-logged-in test account (`testclaudemail762@gmail.com`, chosen
specifically so `login(force:true)` wouldn't evict a *different* account's
session) → verified successfully both times → address saved → search → select.

**Blocked here — two new bugs found, session paused before reaching checkout:**
1. **`extractMenuItems()` returns 0 items for every restaurant tried on Railway**
   (Taco Bell, Tacos Los 3 Hermanos, Little Caesars) — "Prices detected on page
   ✓" but both extraction strategies find nothing, Apollo cache has no
   Menu/Item keys, no SSR `__next_f` fallback fires. Identical `Page height:
   7003px` logged across two different restaurants is suspicious — looks like a
   systemic Railway-vs-Pi environment gap (proxy/CF/render-variant difference),
   not restaurant-specific flakiness like the Pi's known issues. Day 2 the same
   session showed clean extraction on the Pi for 4/5 restaurants, so this is
   new and Railway-specific.
2. **Claude sometimes skips emitting the real `[SELECT: N]` command on a retry
   and hallucinates a plausible-looking menu from its own training data
   instead**, leaving server state pointed at the previous (still-empty)
   restaurant. Caught live: asking to re-pick "Taco Bell" a second time
   produced a full, real-looking Taco Bell menu with no matching `[SELECT]` log
   line at all — `ADD_ITEM_NUM` then failed against the *actual* cached
   restaurant (still 0 items) with a confusing error that contradicted the menu
   just shown. This is a more serious variant of the already-known "Claude
   hallucinating cart/search content" bug class (2026-03-15 notes) — same
   pattern, now confirmed for restaurant menus, and specifically triggered by
   a retry after a failure rather than a fresh request.

Was routing around #1 via the kiosk API (`/api/food/search|select|cart/add`,
same shared browser/session as the SMS test) to reach a real cart item without
depending on the broken SMS-path extraction, then use SMS `[PLACE_ORDER]` for
the actual checkout-preview scrape test. Had just saved a kiosk-side test
address when the session was paused.

**Also worth flagging:** each `[SETUP_DOORDASH]` real-login verification run on
Railway (2 real sign-ins today) sends actual DoorDash 2FA/security codes to
that account's registered contact info — confirmed live when the user reported
receiving DoorDash codes mid-session. Not a bug, just a real side effect of the
2026-09-09 "keep real verification, deploy as-is" decision (see Open, below) —
worth being deliberate about how many test sign-ins happen per session.

**Nothing charged** — no `[CONFIRM_ORDER]` was ever sent; `DOORDASH_DRY_RUN=false`
on Railway (unlike the Pi) so this matters more there than it did in prior
sessions. Two test SMS numbers now exist on Railway with partial onboarding
state (DoorDash linked + address saved, no real cart items) — fine to keep or
wipe next session, nothing sensitive on them.

### Day 5 — `readBrowserCart()` proper fix ⏳ not started
Compounded this week: `readBrowserCart()` / `clearBrowserCart()` `page.evaluate()`
calls are timing out on the Pi even on a cold browser right after a full reboot —
DoorDash's `/home` page hangs evaluates outright on the Pi 4. Needs both an
environmental angle and the parser rewrite the plan already calls for.

## Off-plan work this week (user-requested)

### Kiosk pre-charge confirmation ✅ (committed 2026-09-09, not on Railway)
User wants a confirmation before an order is placed showing the card's last 4 and
the delivery address. SMS already had this (`91a596a`); the kiosk `food.html`
did not — it was a two-tap confirm on a client-side *estimated* total.
- New `POST /api/food/checkout/preview` — loads the real checkout page (places
  nothing), returns delivery address + card `{brand,last4}` + real total, stamps
  `prefs.foodCheckoutPreviewAt`.
- `POST /api/food/checkout` refuses to charge without a preview in the last
  10 min (`428 needsPreview`); the stamp is consumed on any attempt.
- `food.html`: Place Order → preview → one combined confirm panel (Deliver to /
  Pay with Visa ····1234 / Total) → Confirm places. Card-scrape failure falls
  back to "your default DoorDash card" + a caveat line. Preview failure → error,
  no confirm button.
- Verified: gate (`428` with no preview; opens + consumes stamp with one),
  preview endpoint on the Pi (`502` on the degraded browser, does not open the
  gate), and the full `food.html` UI via Playwright with stubbed fetch (confirm
  panel render, 2nd-tap place, triple-tap bounce → 1 preview call, `payment:null`
  fallback, preview-502 error path + retry).
- **Not verified:** a real successful preview — needs Railway.

### SMS: full data wipe for a real user ✅ (done 2026-09-09)
`+18018006072` — the user's real SMS data was on Railway, not the Pi. `/api/clear`
handled conversations + cart + prefs + cache; the rest (address, DoorDash login,
3 orders, 1 cart row) was cleared inside the Railway container via `railway ssh`
(user did `railway login`; temp SSH key registered then removed). Verified:
`isNewUser` recomputes true. `scripts/wipe-user.js` added (reusable full wipe).

### SMS plain-text + onboarding reorder ✅ (committed 2026-09-09, not on Railway)
- `stripSmsMarkdown()` on every SMS reply (not voice) — iMessage was showing
  literal `**` / `*` / `` ` `` / `#`. 11 unit cases pass.
- Onboarding is now two phases: link DoorDash **first** (`setup doordash
  email password`), address **after** the login is verified. Tells the user the
  password is encrypted on arrival, never logged / plaintext / shared.
- `[SETUP_DOORDASH]` runs a real `doordash.login(..., {force:true})` (90s cap)
  and only says "linked and verified" on success; on failure it deletes the
  stored creds and says so.

## Decided 2026-09-11 — real login verification: keep as-is, deploy it

User's call: ship `[SETUP_DOORDASH]`'s real `login(force:true)` verification as
built, accepting that (a) an OTP-gated correct password can falsely report
"couldn't sign in," and (b) verifying a *different* account than whatever the
shared browser is currently on will evict that session. Deployed to Railway
2026-09-11 (`4a02dd0`); today's two test verifications avoided (b) by
deliberately reusing the already-logged-in `testclaudemail762@gmail.com`
account rather than a new one. Real DoorDash 2FA codes land on that account's
contact info per verification attempt — see Day 4 above.

## Open — carried into next session

1. **Railway `extractMenuItems()` returning 0 items** (Day 4 finding #1, above)
   — needs a fresh DOM/Apollo-cache dump from Railway specifically (the Pi's
   working extraction path doesn't transfer over) before it can be fixed.
2. **Claude skipping `[SELECT]` and hallucinating a menu on retry** (Day 4
   finding #2, above) — needs a system-prompt fix (likely a CRITICAL RULE
   parallel to the existing "never describe cart contents yourself" one, but
   for restaurant/menu content) plus a repro that doesn't depend on #1's
   extraction failures to trigger the retry path.
