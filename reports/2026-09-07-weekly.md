# Weekly Report — frog / MessageAI

**Week of Sep 1** · reporting Sun 2026-09-07 · ~6 hours (two sessions: Sun 8/31, Tue 9/1)

Trip mid-week cut the week short — Days 1, 2 and 4 landed, Days 3 and 5 did not start.

---

## Plan vs. actual

| Day | Plan | Status |
|-----|------|--------|
| 1 | Fix the Day-5 HIGH stale-restaurant boot-prewarm hijack | ✅ done — `65739dd` |
| 2 | Fix Little Caesars "Custom Stuffed Crust" incomplete required-options | ✅ done — uncommitted diff |
| 3 | `extractMenuItems()` two-line name truncation + calorie-digit concat | ❌ not started (trip) |
| 4 | MEDIUM audit backlog, pick 2–3 | ✅ done — went well past 2–3 (5 commits) |
| 5 | Full multi-restaurant smoke test + triage | ❌ not started (trip) |

Plus one unplanned feature: SMS pre-charge confirmation + onboarding rewrite (`91a596a`).

---

## Day 1 — stale-restaurant boot-prewarm hijack (`65739dd`, verified live)

The Day-5 smoke test the week before found that a restaurant left cached in the DB from an
old session could hijack a live, unrelated session: on boot the server fired
`prewarmRestaurantPage(cachedRestaurant.url)`, which — even though it holds the browser
lock — navigated the shared page away from the restaurant the user was actively ordering
from, mid-session, and reverted the persisted "current restaurant" back to the stale one.
The user's next add then ran against the wrong store and failed with "0 items found".

Fix: boot prewarm now calls `prewarmBrowser()` (homepage only, harmless) instead of
navigating to a specific stale store. `prewarmRestaurantPage()` also now self-locks and
captures a sequence number synchronously, so a superseded queued prewarm skips its
navigation entirely.

Verified: stale restaurant cached → restart → boot log shows homepage-only, no store nav;
a fresh order (search / select / add) then landed correctly with no hijack; real cart
confirmed empty.

---

## Day 4 — audit backlog (5 commits, 2026-08-31)

The plan asked for 2–3 MEDIUM items; the whole backlog from the 8/25 audit got cleared.

**`e89f971` — isolated module bugs**
- `restaurants.js` `calculateOrderTotal()`: delivery fee is a display string
  (`'Unknown'` / `'$2.99'`), so `.toFixed()` threw or string-concat corrupted the total.
  Added `parseMoney()`. (This path is dead code — only `formatCart` is live — fixed anyway.)
- `modules/reminders.js`: `setTimeout`'s 32-bit ceiling made "in 1000 hours" fire
  immediately → now chained in chunks. Store key coerced to string — JSON string keys vs.
  numeric SQLite ids were making restored reminders invisible to get/cancel while still
  firing.
- `modules/weather.js` `normalizeLocation()`: split-on-whitespace truncated multi-word
  cities ("Salt Lake City, Utah" → "Salt,US") → now takes text before the first comma.
- `spotify.html`: HTML-escape device id/name.

**`0779f3a` — db.js**
- `ENCRYPTION_KEY` was presence-checked only; a malformed key threw on the first
  `encrypt()` mid-request. Now requires exactly 64 hex chars at boot.
- PINs were unsalted single-round SHA-256 → now scrypt + per-user salt, with transparent
  upgrade of any legacy hash on next successful verify. (Feature is dormant, no live rows.)
- `getOrder` / `getUserOrders`: unguarded `JSON.parse` → guarded, returns `[]` on bad data.

**`6c76372` — server.js**
- `_checkoutInFlight` single-flight guard: a second concurrent real checkout across the
  touchscreen route and the voice `[PLACE_ORDER]` path is now 409'd, not queued.
- `/api/user/:phoneNumber` was gated on `req.ip`, spoofable via `X-Forwarded-For` → now
  raw socket address or an explicit debug token.
- `[REORDER]` now tells the user which items it couldn't re-add.
- Final-sweep strips residual ALL-CAPS `[BRACKET]` tags the model sometimes doubles.

**`cff2d6f` — checkout honesty + real totals**
- `checkoutCurrentCart()` / `placeOrder()` used to *assume success* when they couldn't
  recognize the confirmation page. Now `checkoutCurrentCart` re-checks whether the Place
  Order button is still live (still there = order didn't go, cart intact, safe retry);
  button gone but unconfirmed → flagged `unconfirmed` and the user is told to check the
  DoorDash app rather than shown "Order placed!".
- Real order totals: subtotal / fees / grand total are now scraped off the checkout page
  and the Place Order button, and the saved order record + user reply use those real
  numbers when present.
- `applyOptionSelections()` no longer accepts "required-count is 0" as success without
  confirming the *right* option is selected.
- ~15 more naked `page.evaluate()` calls bounded with `evalWithTimeout()`.
- `readBrowserCart()` name parsing: was taking the entire cart-row text as the item name
  when no name node matched → added `h3/h4/ItemName` selectors + a fallback that cuts at
  the first `$` / calorie / quantity.

Verified live: search / select (Wingstop, 39 clean items) / add with a required option
group ("Cajun Fried Corn" → "Choose Size") landed correctly in the real cart, no
regression.

**Remaining audit item (not done):** the saved-order record is still built from the local
cart rather than the real DoorDash cart.

---

## Unplanned — SMS pre-charge confirmation + onboarding (`91a596a`, SMS only)

SMS `[PLACE_ORDER]` no longer charges directly. It now previews the real checkout page —
delivery address, payment card brand + last-4, and total — and replies "Reply confirm or
cancel. Nothing's been charged yet." A new `[CONFIRM_ORDER]` does the real placement
(15-min TTL on the pending checkout); `[CANCEL_ORDER]` clears it. Voice and kiosk keep
one-shot checkout unchanged. New-user onboarding rewritten from a stub into a 2-message
walkthrough: what it is → how ordering works → how checkout stays safe → setup steps.

Verified: fresh number gets the full walkthrough; "checkout" routes through the preview
path with no order placed. **Not verified:** the actual address/card/total scrape output
and real checkout placement — DoorDash's checkout page wouldn't load in the Pi's degraded
browser this session and `DOORDASH_DRY_RUN=true` is set on the Pi. The scrape targets the
same page the Railway MessageAI deploy already uses.

---

## Day 2 — Little Caesars "Custom Stuffed Crust" required options (2026-09-01, verified live, uncommitted)

Build-your-own pizzas were surfacing only one generic option group ("Your recommended
options" = sauce amount) instead of the real required Sauce + Cheese groups, so cheese got
auto-defaulted server-side and the user never picked it.

The modal never opens via click for these items, so DOM extraction can't see the groups.
Going the GraphQL route instead surfaced **three real bugs**, all now fixed:

1. **`itemPage` GraphQL query was stale** — still sent the `isNested` argument, which
   DoorDash removed. Every call 400'd `GRAPHQL_VALIDATION_FAILED`, so the HTTP
   options fast-path had been **dead for every optioned item at every restaurant**,
   always silently falling back to Playwright DOM scraping.
2. **`extractStoreIdFromUrl` returned the menu id, not the store id** for canonical
   `/store/<slug>-<storeId>/<menuId>/` URLs — so item-id lookups always missed and
   `fetchItemPageViaAPI` 404'd.
3. **SSR-only stores expose no item id in the DOM or React fiber** — Little Caesars puts
   them only in DoorDash's server-streamed `__next_f` payload. Added a parser that pulls
   `{id, name}` pairs from that payload and stamps the real id onto the cached menu so it
   survives a restart.

Also: `convertOptionListsToRequired` now surfaces genuinely-required groups that DoorDash
ships as `isOptional:false / minNumOptions:0`, and discovery + payload-builder now use the
same rule so we don't add-then-fail on a group we never asked about.

Verified live against the real cart, twice — including once after a full restart with an
empty in-memory id cache, proving the persisted-id path — "Custom Stuffed Crust" with
Sauce=Regular + Cheese=Regular added via the HTTP API at the correct $17.49 base, no wrong
upcharge.

**Known rough edge (reported, left for later):** Costa Vida "Single Taco" add still fails —
DoorDash's `itemPage` response *omits* the required "Tortilla" group entirely, so the API
add is rejected and the Playwright fallback hits the pre-existing Costa Vida
menu-render flakiness. Never worked before this change either.

---

## Repo / deploy state

- 6 commits `65739dd..91a596a` on `master`, **unpushed**.
- Day-2 `doordash.js` fix is a **working-tree diff, uncommitted and unpushed** (deployed to
  the Pi and verified live).
- `modules/gps.js` still uncommitted (GPS software done; hardware is the bare-UART variant,
  waiting on wiring).
- `DOORDASH_DRY_RUN=true` on the Pi — no checkout path this week could have charged.
- Both services healthy; real DoorDash cart confirmed empty at each session end.

---

## Post-week catch-up — Sun Sep 7

Back from the trip. Cleared two of the three items that slipped:

- **Day-2 fix committed** — `604a267`. Verified on the Pi (still live).
- **Day 3 done** — `4be5269`. Added a shared `cleanScrapedName()` applied at the Node
  boundary of every DOM-scrape fallback (`extractMenuItems`, `extractRequiredOptions`).
  It repairs the three observed shapes: `"Tacos -"`, `"10 Classic Wings860 -"`,
  `"Small Chips & Salsa+"`. Verified live: fresh Taco Time (40 items) and Costa Vida
  (22 items) scrapes have zero mangled names; Costa Vida "Chips & Salsa" → "Size" →
  "Small Chips & Salsa" adds end-to-end into the real cart at $4.49. 24 unit cases pass.
  Note: the menu-item `" -"` shape no longer reproduces on its own — DoorDash rewrote the
  card DOM and the SSR `__next_f` path (Day 2) supplies clean names now — so this is
  mostly hardening the fallback for when the API path fails.
- **8 commits now unpushed** (`65739dd..4be5269`), still holding for the go-ahead.

Still open, rolled into the week-of-Sep-8 plan:

- Day 4's last item — the saved-order record is still built from the local cart.
- Day 5 — the multi-restaurant smoke test.
- Costa Vida "Single Taco" — `itemPage` omits the Tortilla group.
- `readBrowserCart()` name-mash — seen live today:
  `"Chips & SalsaSmall Chips & Salsa, Pico De Gallo$4.491 ×1 ×1 × - $0.00"`.
- `clearBrowserCart()` slow / unreliable on the degraded Pi browser (didn't fully clear
  a test item today; `DRY_RUN` is on so no money risk).
- Verify the SMS confirmation scrape (address / card / total) end-to-end on Railway.
- SMS `[SELECT]` doesn't update `currentRestaurant` on a second select in one session.

---

# Quant Trading System — Phases 0 & 1 (new project, started Sun Sep 7)

*Separate from frog — its own repo (`github.com/hatchatcha9/quant-system`, private) and
its own roadmap (`BUILD_PLAN.md` in that repo). Included here because it's the same
reporting week.*

**Time:** ~3.5 h, one session (Sun 9/7) — estimate. Covers the full phased plan, a
shareable plan page, and Phases 0 + 1.

A learning-first quantitative trading system in Python, built in 7 gated phases (0–6):
data pipeline → honest backtester → trained model → a math-expert + crypto agent
ensemble → paper trading → a gated ~$100 real-money test. Profit is a secondary goal;
the point is fluency in data pipelines, simulation, testing, and statistical honesty.
Built around crypto (spot) so there are no market-hours edge cases. Two phases landed
this session.

| Phase | Plan | Status |
|-------|------|--------|
| 0 | Tested, runnable skeleton | ✅ done — `v0.0-scaffold` (`deaed31`) |
| 1 | Data pipeline into local SQLite | ✅ done — `v0.1-data` (`f75c99d`) |

## Phase 0 — foundations (`v0.0-scaffold`, `deaed31`)

`uv` for env/deps, `src/quant/` package (src-layout, editable install), `ruff` (lint +
format), `mypy --strict` on `src`, `pytest`. `config.py` loads typed settings from
`.env` — every secret is optional and only required where used; `LIVE_TRADING_ENABLED`
defaults `false` with a test locking that in. GitHub Actions CI runs the four checks on
every push. Private repo created and pushed.

Definition of Done met: a fresh clone passes `pytest` / `ruff check` / `ruff format
--check` / `mypy src`, and CI is green.

## Phase 1 — data pipeline (`v0.1-data`, `f75c99d`)

`src/quant/data/`, four modules, 31 tests:

- **`schema.sql`** — OHLCV `bars` table. Composite primary key `(symbol, timeframe,
  timestamp_utc)` makes a re-fetch an update, not a duplicate. Timestamps stored as
  ISO-8601 UTC text.
- **`store.py`** — `connect()` (commits + closes; stdlib sqlite3 only manages the
  transaction), `init_db()`, `upsert_bars()` (rejects a naive-timezone index loudly),
  `load_bars()` (UTC-indexed; inclusive date bounds normalised through `pd.Timestamp` so
  a bare-string bound can't mis-compare against stored timestamps).
- **`fetchers.py`** — `with_retry()` decorator (exponential backoff + jitter, only
  retries listed exceptions), `_normalize_yfinance_frame()` (a pure transform that
  absorbs yfinance's version-to-version shape drift: flat vs MultiIndex columns, tz-aware
  vs naive index, extra `Adj Close`, incomplete rows), `fetch_yfinance()`, and a CLI.
- **`quality.py`** — `sanity_check()` (reports bad OHLC rows, doesn't raise) and
  `find_gaps()` (missing days — crypto trades every day, so any missing date is real).

Verified live: `python -m quant.data.fetchers BTC-USD --start 2019-01-01` fetched 2807
daily bars (2019-01-01 → 2026-09-07), 0 gaps, sanity clean; a second run left the row
count at 2807 (idempotent) with real data. `data/quant.sqlite` is gitignored.

## CI caught three real issues (all fixed before "done")

Local checks were green; CI failed three times, each a real problem:

1. **`ruff` import-sorting differed local vs CI** — ruff's first-party auto-detection
   classified the `quant` package differently on the two machines. Pinned
   `known-first-party = ["quant"]` in config. (`483d8a1`)
2. **The whole `src/quant/data/` package was untracked.** `.gitignore` had `data/` (no
   leading slash) for the local database directory, which also matched
   `src/quant/data/`. `git status` stayed clean the whole time because ignored files
   don't show, and `ruff` respects `.gitignore` so it was silently skipping that code
   too — the local green bar was a lie. Anchored the rule to `/data/`. (`9a19a37`)
3. A long-line lint error that only surfaced once ruff could see those files. (`f75c99d`)

Lesson kept: anchor `.gitignore` directory rules to `/name/` when a same-named directory
can exist deeper in the tree; verify a phase against a throwaway `git clone` + `uv sync`,
which is what CI actually does.

## State

- 10 commits `5826b6f..f75c99d` on `main`, **all pushed**, CI green, tree clean.
- Tags `v0.0-scaffold` (`deaed31`) and `v0.1-data` (`f75c99d`).
- ~410 lines of Python in `src/`, 31 tests.
- No real money anywhere near this — Phase 6 ($100) is five phases out and gated on the
  ensemble beating buy-and-hold net of fees on held-out data **and** in forward paper.

## Next — Phase 2 (backtester + SMA baseline)

The intellectual core: a bar-by-bar simulator where the strategy interface structurally
can't see the future (it only ever receives bars up to time `t`), fills happen at the
next bar's open, and commission + slippage are on by default. First deliverable is the
interface plus a lookahead cheat-test that tries to peek and asserts it can't. This is
also the baseline — a plain moving-average crossover — that every later phase (model,
agents) has to beat after costs. Milestone-gated, no day allocation.
