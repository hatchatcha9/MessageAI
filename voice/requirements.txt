# PiAI Voice Layer Dependencies
# Install: pip install -r requirements.txt

# Core
faster-whisper>=1.0.0       # Speech-to-text (Whisper, optimized for Pi)
sounddevice>=0.4.6          # Mic input / audio playback
numpy>=1.24.0               # Audio processing
requests>=2.31.0            # HTTP to Node.js server

# TTS — Kokoro neural voices (male: am_michael, female: af_heart)
# Switch voice by setting env var: PIAI_VOICE=male or PIAI_VOICE=female
kokoro>=0.9.4               # Neural TTS, works on Windows & Pi
soundfile>=0.12.1           # Audio file I/O for Kokoro

# Wake word (optional, used on Pi in production mode)
# openwakeword>=0.6.0       # Uncomment when deploying to Pi
